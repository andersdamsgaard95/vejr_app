Kørsel af app:

Appen skal køres via en lokal server, fordi geolokation (navigator.geolocation) ikke virker direkte fra filsystemet. Start fx en server med:

'npx serve' i terminalen.

åbn derefter URL’en, som serveren viser (typisk http://localhost:3000).
